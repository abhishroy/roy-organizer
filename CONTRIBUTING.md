# Contributing

Use Python 3.10+, keep `planning_only=true`, and run
`python -m unittest discover -s tests -v`. Filesystem tests must use temporary
directories. Run `roy demo` before proposing executor changes. Never commit
generated inventories, real paths, credentials, company data, or private files.
