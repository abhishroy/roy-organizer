# CLI

`roy` opens the terminal home. Advanced read-only commands include `scan`,
`report`, `review`, `protected`, `duplicates --report`, `score`, `storage`,
`status`, and `doctor`. `gui` opens the plan-only preview. `demo` is the only
execute/undo experience and operates in a temporary `/tmp` tree. Real `organize`
and `undo` remain blocked.
