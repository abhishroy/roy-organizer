# Sandbox

`roy demo` creates Desktop, Downloads, Documents, Pictures, Movies, Projects,
synthetic screenshots/media/archives/configs/projects, and no credentials. It runs
Scan-style data preparation → approved operation → final validation → sandbox move
→ metadata log → verification → undo → integrity verification. The root is under
`/tmp` and is automatically removed.
