# Privacy

ROY scans locally. It has no telemetry and requires no cloud service. Generated
inventories, plans, logs, and reports are gitignored. Optional Ollama support is
off by default, local-only, and sends only a filename to an already-installed
local model. ROY never uploads filenames or document contents.
